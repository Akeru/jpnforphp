{% extends 'layout.twig' %}
{% block body %}
    <div class="container">
        {% if error is defined %}
            <p>Error : {{ error }}</p>
        {% endif %}
        <h3>Analyser demos</h3>

        <p>&nbsp;</p>

        <div class="well">
            <form class="form-horizontal" method="post" action="/analyzer">
                <div class="form-group">
                    <label for="string" class="col-sm-2 control-label">Analyze string</label>

                    <div class="col-sm-10">
                        <input type="text" class="form-control" id="string" name="string" required>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                        <button type="submit" class="btn btn-default">Submit</button>
                    </div>
                </div>
            </form>
        </div>
        <p>&nbsp;</p>
        <a href="/">Back</a>
    </div>
{% endblock %}
